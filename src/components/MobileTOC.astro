---
---

<!-- Mobile TOC Toggle (always available on small screens) -->
<div class="mb-6 lg:hidden">
  <button
    id="mobileTOCToggle"
    class="flex items-center gap-2 text-sm font-medium transition-colors mb-3"
    style="color: var(--accent-color);"
  >
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
    </svg>
    <span id="mobileTOCToggleText">Show Contents</span>
  </button>
  <div id="mobileTOC" class="hidden mt-3 flex flex-col gap-2"></div>
</div>
